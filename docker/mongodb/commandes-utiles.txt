

docker-compose up --build
OR
docker-compose up --build -d eceproject-mongo 

// Commande Mongo Shell
docker exec -it eceproject-mongo mongo

// Save
docker exec eceproject-mongo sh -c 'mongodump --archive' > db.dump

// Restore
docker exec -i eceproject-mongo sh -c 'mongorestore --archive' < db.dump

// Create container for node app
docker image build -t node-project .

//Purge the linux pb mongo server
sudo dpkg -i --force-all /var/cache/apt/archives/mongodb-org-mongos_4.2.1_amd64.deb

sudo dpkg -i --force-all /var/cache/apt/archives/mongodb-org-server_4.2.1_amd64.deb

//Lancer le service Mongo
sudo service mongod start


//Lancer le bash dans le container
docker exec -it eceproject-web bash
